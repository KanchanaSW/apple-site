export enum SelectedPage {
    Home = "home",
    About = "about",
    Products= "products",
    Features= "features",
    Contact = "contact",
  }

  export interface NewArrivalType {
    icon:string;
    title:string;
    description:string;
  }

  export interface ClassType{
    name:string;
    description:string;
    image:string;
  }